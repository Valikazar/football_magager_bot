<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Footbot Championships</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
</head>
<body>
    <div class="container">
        <h1>Footbot Championships</h1>
        
        <div class="grid">
            <% championships.forEach(champ => { %>
                <a href="/championship?chat_id=<%= champ.chat_id %>&thread_id=<%= champ.thread_id %>" class="card champ-card">
                    <div class="champ-header">
                        <div class="icon">⚽</div>
                        <div>
                            <div style="font-weight: 600;">Championship</div>
                            <div style="font-size: 0.875rem; color: var(--text-muted);">ID: <%= champ.chat_id %> / <%= champ.thread_id %></div>
                        </div>
                    </div>
                    <div style="color: var(--text-muted); font-size: 0.875rem;">
                        Click to view stats and matches
                    </div>
                </a>
            <% }); %>
        </div>
        
        <% if (championships.length === 0) { %>
            <div class="card" style="text-align: center; color: var(--text-muted);">
                No championships found in the database.
            </div>
        <% } %>
    </div>
</body>
</html>
